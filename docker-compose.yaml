services:
  server:
    build: 
      context: .
      # run up to production stage
      target: production
    environment:
      NODE_ENV: production
    ports:
      - 5757:5757 # web server port
    volumes:
      - ./data:/usr/src/app/data
